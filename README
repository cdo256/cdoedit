cdoedit - simple text editor
--------------------
cdoedit is a simple text file editor for X intended only for my personal use.


Requirements
------------
In order to build cdoedit you need the Xlib header files.


Installation
------------
Edit config.mk to match your local setup (cdoedit is installed into
the /usr/local namespace by default).

Afterwards enter the following command to build and install cdoedit (if
necessary as root):

    make clean install


Credits
-------
Based on Suckless Simple Terminal
Based on Aurélien APTEL <aurelien dot aptel at gmail dot com> bt source code.
